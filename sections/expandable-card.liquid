{{ 'section-expandable-card.css' | asset_url | stylesheet_tag }}
<script src="{{ 'section-expandable-card.js' | asset_url }}" defer="defer" type="module"></script>
{%- liquid
  assign section_st = section.settings
  assign theme_st = settings
  assign scroll_animation = theme_st.scroll_animation

  if section_st.section_width == 'fix_width'
    assign section_width = 'container'
  elsif section_st.section_width == 'full_width'
    assign section_width = 'full-width'
  else
    assign section_width = 'fluid_container'
  endif

  assign items_per_row = section_st.items_per_row
  assign show_pagination = section_st.show_pagination
  assign show_navigation = section_st.show_navigation
  assign slide_auto_change = section_st.slide_auto_change
  assign change_slide_every = section_st.change_slide_every
  assign reveal_slide = section_st.reveal_slide
  assign image_ratio = section_st.image_ratio
  assign interaction_behavior = section_st.interaction_behavior

  assign column_gap_slide = 0
  assign column_gap_mb = 0
  if section_st.column_gap == 'large'
    assign column_gap_slide = 32
    assign column_gap_mb = 24
  elsif section_st.column_gap == 'medium'
    assign column_gap_slide = 24
    assign column_gap_mb = 12
  elsif section_st.column_gap == 'small'
    assign column_gap_slide = 16
    assign column_gap_mb = 8
  endif

  assign heading = section_st.heading
  assign drawn_underline = section_st.drawn_underline
  assign drawn_underline_type = section_st.drawn_underline_type
  assign drawn_underline_color = section_st.drawn_underline_color
  if drawn_underline
    assign scroll_animation = 'disable'
  endif

  assign items_per_row_mobile = section_st.items_per_row_mobile
  assign data_free_scroll = 'false'
  if items_per_row_mobile > 1 and items_per_row_mobile < 2
    assign data_free_scroll = 'true'
  elsif items_per_row_mobile > 2 and items_per_row_mobile < 3
    assign data_free_scroll = 'true'
  endif

  assign items_per_row_tablet = items_per_row
  if items_per_row > 3
    assign items_per_row_tablet = items_per_row | minus: 1
  endif

  assign heading_size = section_st.heading_size
  assign header_size = ''
  if heading_size == 'small'
    assign header_size = 'h3'
  elsif heading_size == 'large'
    assign header_size = 'h1'
  endif

  assign class_pagination = ''
  if show_pagination == false
    if items_per_row_mobile == 1 or items_per_row_mobile == 2
      assign class_pagination = ' hidden-md block'
    elsif items_per_row_mobile > 1 and items_per_row_mobile < 2
      assign class_pagination = ' hidden'
    endif
  endif

  assign interaction_class = 'expandable-card-section--hover'
  if interaction_behavior == 'drawer'
    assign interaction_class = 'expandable-card-section--drawer'
  endif
-%}

{%- capture style -%}
  --section-pt: {{ section_st.padding_top }};
  --section-pb: {{ section_st.padding_bottom }};
{%- endcapture -%}

{%- capture col_style -%}
  --item-desktop: {{ items_per_row }};--item-tablet: {{ items_per_row_tablet }};--item-mb: {{ items_per_row_mobile }};--auto-speed: {{ change_slide_every }}s;
  --gap-desktop: {{ column_gap_slide }}px;--gap: {{ column_gap_mb }}px;
{%- endcapture -%}

<div
  class="section expandable-card-section {{ interaction_class }}{% if reveal_slide %} overflow-hidden{% endif %} color-{{ section_st.color_scheme }} gradient{% if section_st.show_divider %} border-top{% endif %}{% if slide_auto_change %} autoplay{% endif %}"
  style="{{ style | strip | strip_newlines }}"
  data-interaction="{{ interaction_behavior }}"
>
  <div class="{{ section_width }}">
    {%- if heading != blank or section_st.description != blank or section_st.subheading != blank -%}
      <div class="section-header text-{{ section_st.header_alignment }}">
        {%- assign animate_delay = 25 -%}
        {%- if section_st.subheading != blank -%}
          {%- if scroll_animation != 'disable' -%}
            <motion-effect data-animate="fade-up" data-animate-delay="{{ animate_delay }}" class="block">
          {%- endif -%}
          <div class="section-subheading h7 mb-sp-1 mb-md-sp-2 subheading_weight fs-big-2">
            {{ section_st.subheading }}
          </div>
          {%- if scroll_animation != 'disable' -%}
            </motion-effect>
          {%- endif -%}
        {%- endif -%}

        {%- if heading != blank -%}
          {%- assign animate_delay = animate_delay | plus: 50 -%}
          {%- if scroll_animation != 'disable' or drawn_underline -%}
            <motion-effect
              data-animate="{% if scroll_animation != 'disable' %}fade-up{% else %}highlighted-text{% endif %}"
              data-animate-delay="{{ animate_delay }}"
              class="block"
            >
          {%- endif -%}
          <h2 class="block section-heading {{ header_size }}">
            {%- if drawn_underline -%}
              {%- capture squiggle_svg_1 -%}
                <svg viewBox="0 0 184 26" fill="none"><use href="#squiggle-svg-1" /></svg>
              {%- endcapture -%}
              {%- capture squiggle_svg_2 -%}
                <svg viewBox="0 0 186 24" fill="none"><use href="#squiggle-svg-2" /></svg>
              {%- endcapture -%}
              {%- capture squiggle_svg_3 -%}
                <svg viewBox="0 0 186 24" fill="none"><use href="#squiggle-svg-3" /></svg>
              {%- endcapture -%}
              {%- capture squiggle_svg_4 -%}
                <svg viewBox="0 0 220 20" fill="none" role="presentation" preserveAspectRatio="none"><use href="#squiggle-svg-4" /></svg>
              {%- endcapture -%}
              {%- capture squiggle_svg_5 -%}
                <svg viewBox="0 0 140 24" fill="none" role="presentation" preserveAspectRatio="none"><use href="#squiggle-svg-5" /></svg>
              {%- endcapture -%}
              {%- assign parts = heading | split: '<em>' -%}
              {%- assign em_count = parts | size | minus: 1 -%}
              {%- for i in (1..em_count) -%}
                {%- assign idx = i | plus: 0 -%}
                {%- assign chunk = parts[idx] | default: '' -%}
                {%- assign text = chunk | split: '</em>' | first | strip_html | strip -%}
                {%- assign len_text = text | size -%}
                {%- capture open_em -%}
                {%- liquid
                  if drawn_underline_type == 'simple_underline'
                    assign svg = squiggle_svg_4 | append: '</em>'
                  elsif drawn_underline_type == 'curve_underline'
                    assign svg = squiggle_svg_5 | append: '</em>'
                  else
                    if len_text <= 4
                      assign svg = squiggle_svg_1 | append: '</em>'
                    elsif len_text <= 6
                      assign svg = squiggle_svg_2 | append: '</em>'
                    else
                      assign svg = squiggle_svg_3 | append: '</em>'
                    endif
                  endif
                -%}
                <em class="highlighted-text relative inline-flex{% if drawn_underline_type == 'simple_underline' %} simple_underline{% endif %} {{ drawn_underline_type }} not-italic" style="--animate-color:{{ drawn_underline_color }}">{{ svg }}</em>
              {%- endcapture -%}
                {%- assign open_em = open_em | remove: '</em>' -%}
                {%- assign heading = heading | replace_first: '<em>', open_em -%}
              {%- endfor -%}
            {%- endif -%}
            {{- heading -}}
          </h2>
          {%- if scroll_animation != 'disable' or drawn_underline -%}
            </motion-effect>
          {%- endif -%}
        {%- endif -%}

        {%- if section_st.description != blank -%}
          {%- assign animate_delay = animate_delay | plus: 50 -%}
          {%- if scroll_animation != 'disable' -%}
            <motion-effect data-animate="fade-up" data-animate-delay="{{ animate_delay }}" class="block">
          {%- endif -%}
          <div class="section-description mt-sp-4 prose">
            {{ section_st.description }}
          </div>
          {%- if scroll_animation != 'disable' -%}
            </motion-effect>
          {%- endif -%}
        {%- endif -%}
      </div>
    {%- endif -%}

    <slide-section
      class="swiper hover-action expandable-card__slider{% if reveal_slide %} reveal_on_scroll{% endif %}{% if data_free_scroll == 'true' %} overflow-visible-mb{% endif %}"
      data-section-id="{{ section.id }}"
      data-effect="slide"
      data-loop="false"
      data-speed="300"
      data-autoplay="{{ slide_auto_change }}"
      data-auto-play-speed="{{ change_slide_every | times: 1000 }}"
      data-pause-on-mouse-enter="true"
      data-spacing="{{ column_gap_slide }}"
      data-mobile="{{ items_per_row_mobile }}"
      data-desktop="{{ items_per_row }}"
      data-tablet="{{ items_per_row_tablet }}"
      data-free-scroll="{{ data_free_scroll }}"
      style="{{ col_style | strip | strip_newlines }}"
      data-arrow-center-image="true"
    >
      {%- if show_navigation -%}
        {%- render 'swiper-navigation', show_on_hover: true -%}
      {%- endif -%}
      <div class="swiper-wrapper">
        {%- for block in section.blocks -%}
          {%- liquid
            assign block_st = block.settings
            assign drawer_page = block.settings.page
            assign image = block_st.image
            assign image_mobile = block_st.image_mobile
            assign image_alt = image.alt | default: block_st.heading | escape
            assign ratio = '3/4'
            assign ratio_mobile = '3/4'

            if image_ratio != 'adapt'
              case image_ratio
                when 'square'
                  assign ratio = '1/1'
                  assign ratio_mobile = '1/1'
                when 'landscape'
                  assign ratio = '4/3'
                  assign ratio_mobile = '4/3'
                when 'portrait'
                  assign ratio = '3/4'
                  assign ratio_mobile = '3/4'
              endcase
            else
              if image != blank
                assign ratio = image.aspect_ratio
                if image_mobile != blank
                  assign ratio_mobile = image_mobile.aspect_ratio
                else
                  assign ratio_mobile = ratio
                endif
              elsif image_mobile != blank
                assign ratio = image_mobile.aspect_ratio
                assign ratio_mobile = image_mobile.aspect_ratio
              endif
            endif
            assign drawer_id = 'ExpandableCardDrawer-' | append: section.id | append: '-' | append: block.id
          -%}
          <div class="swiper-slide" {{ block.shopify_attributes }}>
            <expandable-card
              class="expandable-card block rounded overflow-hidden relative h-full"
              data-behavior="{{ interaction_behavior }}"
              data-drawer-template="{{ drawer_id }}"
              style="--aspect-ratio-mb: {{ ratio_mobile }};--aspect-ratio: {{ ratio }};"
            >
              <div class="expandable-card__media absolute inset-0 overlay-bg overflow-hidden hover-effect">
                {%- if image != blank or image_mobile != blank -%}
                  {%- render 'responsive-image',
                    type: 'banner',
                    image: image,
                    image_mobile: image_mobile,
                    image_alt: image_alt,
                    column: items_per_row,
                    column_mobile: items_per_row_mobile,
                    padding: column_gap_slide,
                    class: 'absolute inset-0 w-full h-full object-fit-cover',
                    motion: false
                  -%}
                {%- else -%}
                  {%- render 'placeholder-render',
                    type: 'lifestyle',
                    index: forloop.index,
                    class: 'img-placeholder absolute inset-0 w-full h-full',
                    motion: false
                  -%}
                {%- endif -%}
              </div>
              <div class="expandable-card__content absolute inset-x-0 bottom-0 z-2 flex column overflow-x-hidden overflow-y-auto custom-scrollbar max-h-100">
                <div
                  class="expandable-card__text grow-1 text-start flex space-between align-center min-w-custom"
                  style="--min-width: 0;"
                >
                  <div>
                    {%- if block_st.subheading != blank -%}
                      <div class="expandable-card__subheading fs-small mb-sp-1 opacity" style="--opacity: 0.8">
                        {{ block_st.subheading }}
                      </div>
                    {%- endif -%}
                    {%- if block_st.heading != blank -%}
                      <h3 class="expandable-card__heading h5 my-0">
                        {{ block_st.heading }}
                      </h3>
                    {%- endif -%}
                  </div>
                  <button
                    type="button"
                    class="expandable-card__toggle btn-reset rounded-50 flex content-center flex-auto"
                    aria-label="{{ block_st.heading | escape }}"
                    aria-haspopup="{% if interaction_behavior == 'drawer' %}dialog{% else %}false{% endif %}"
                    aria-expanded="false"
                  >
                    <span class="open-children-toggle transition relative icon-plus" aria-hidden="true">
                      <span class="icon_plus-animation heading-color"></span>
                    </span>
                  </button>
                </div>
                {%- if block_st.description != blank -%}
                  <div class="expandable-card__description rte rich__text-m0 grid opacity-0">
                    <div class="expandable-card__description-inner overflow-hidden">
                      {{ block_st.description }}
                    </div>
                  </div>
                {%- endif -%}
              </div>
              {%- if interaction_behavior == 'drawer' -%}
                <template id="{{ drawer_id }}">
                  <div
                    class="modal transition-drawer invisible fixed inset-0 z-22 pointer-none active-modal-js content-hidden"
                    drawer
                  >
                    <modal-overlay class="modal-overlay cursor-crosshair"></modal-overlay>
                    <div
                      class="modal-inner transition-drawer modal-body modal-draggable color-default drawer drawer-right draw-mb drawer-right-mb fixed right-0 z-2 inset-y-0 w-full flex column bg-white overflow-hidden"
                      tabindex="-1"
                      role="dialog"
                      aria-modal="true"
                      aria-label="{{ block_st.heading | escape }}"
                      style="--drawer-width: 48rem;"
                    >
                      <draggable-modal
                        class="drawer__header draggable-modal flex gap-10 space-between align-center drawer-spacing border-bottom sticky hidden-drag"
                        style="--padding-block:1.2rem;--padding-block-desktop: 1.7rem;--padding-inline-desktop:3.2rem;"
                      >
                        <div class="drawer__heading h5 flex gap align-center" style="--gap-desktop: 8px;--gap: 8px">
                          {{ block_st.heading }}
                        </div>
                        <button
                          is="button-close-model"
                          class="modal__close modal-button-close rounded-50 pointer btn-reset h-full flex content-center"
                          aria-label="{{ 'accessibility.close' | t }}"
                        >
                          <svg
                            width="12"
                            height="12"
                            viewBox="0 0 14 14"
                            fill="none"
                            class="transition-short will-change icon-zoom"
                          >
                            <use href="#icon-close"></use>
                          </svg>
                        </button>
                      </draggable-modal>
                      <div class="drawer__body grow-1 h-full overflow-y-auto overflow-x-hidden custom-scrollbar drawer-spacing">
                        <div class="expandable-card-drawer__description rte rich__text-m0">
                          {% if drawer_page != null and pages[drawer_page].content %}
                            {{ pages[drawer_page].content }}
                          {% else %}
                            {{ block.settings.description }}
                          {% endif %}
                        </div>
                      </div>
                    </div>
                  </div>
                </template>
              {%- endif -%}
            </expandable-card>
          </div>
        {%- endfor -%}
      </div>
      <div class="swiper-pagination {{ class_pagination }}"></div>
    </slide-section>
  </div>
</div>

{% schema %}
{
  "name": "t:sections.expandable_card.name",
  "tag": "section",
  "class": "section section-expandable-card",
  "disabled_on": {
    "groups": ["header", "footer", "custom.overlay"]
  },
  "max_blocks": 12,
  "settings": [
    {
      "type": "paragraph",
      "content": "t:sections.expandable_card.how_to_use"
    },
    {
      "type": "header",
      "content": "t:sections.all.general"
    },
    {
      "type": "select",
      "id": "section_width",
      "label": "t:sections.all.layout.label",
      "default": "fix_width",
      "options": [
        { "value": "fix_width", "label": "t:sections.all.layout.fix_width" },
        { "value": "stretch_width", "label": "t:sections.all.layout.stretch_width" },
        { "value": "full_width", "label": "t:sections.all.layout.full_width" }
      ]
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "info": "t:sections.all.colors.info",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "t:sections.all.content_header.label"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "t:sections.all.content_setting.subheading.label",
      "default": "Plant-Powered Care"
    },
    {
      "type": "richtext",
      "id": "heading",
      "label": "t:sections.all.content_setting.heading.label",
      "default": "<p>Botanical Ingredients</p>"
    },
    {
      "type": "checkbox",
      "id": "drawn_underline",
      "default": true,
      "label": "t:sections.all.content_setting.drawn_underline.label",
      "info": "t:sections.all.content_setting.heading.info"
    },
    {
      "type": "select",
      "id": "drawn_underline_type",
      "label": "t:sections.all.content_setting.drawn_underline.type",
      "default": "squiggle_underline",
      "visible_if": "{{ section.settings.drawn_underline != false }}",
      "options": [
        {
          "value": "simple_underline",
          "label": "t:sections.all.content_setting.drawn_underline.value.simple_underline"
        },
        {
          "value": "curve_underline",
          "label": "t:sections.all.content_setting.drawn_underline.value.curve_underline"
        },
        {
          "value": "squiggle_underline",
          "label": "t:sections.all.content_setting.drawn_underline.value.squiggle_underline"
        }
      ]
    },
    {
      "type": "color",
      "id": "drawn_underline_color",
      "label": "t:sections.all.content_setting.drawn_underline.color",
      "default": "#FFB005",
      "alpha": true,
      "visible_if": "{{ section.settings.drawn_underline != false }}"
    },
    {
      "type": "richtext",
      "id": "description",
      "label": "t:sections.all.content_setting.description.label"
    },
    {
      "type": "select",
      "id": "heading_size",
      "label": "t:sections.all.content_header.heading_size.label",
      "default": "medium",
      "options": [
        {
          "value": "large",
          "label": "t:sections.all.content_header.heading_size.options__1.label"
        },
        {
          "value": "medium",
          "label": "t:sections.all.content_header.heading_size.options__2.label"
        },
        {
          "value": "small",
          "label": "t:sections.all.content_header.heading_size.options__3.label"
        }
      ]
    },
    {
      "type": "select",
      "id": "header_alignment",
      "label": "t:sections.all.text_alignment.alignment",
      "default": "center",
      "options": [
        { "value": "start", "label": "t:sections.all.text_alignment.options__1.label" },
        { "value": "center", "label": "t:sections.all.text_alignment.options__2.label" },
        { "value": "end", "label": "t:sections.all.text_alignment.options__3.label" }
      ]
    },
    {
      "type": "header",
      "content": "t:sections.expandable_card.settings.card_header"
    },
    {
      "type": "select",
      "id": "image_ratio",
      "label": "t:sections.all.image.aspect_ratio.label",
      "default": "portrait",
      "options": [
        { "value": "adapt", "label": "t:sections.all.image.aspect_ratio.adapt" },
        { "value": "square", "label": "t:sections.all.image.aspect_ratio.options__1" },
        { "value": "portrait", "label": "t:sections.all.image.aspect_ratio.options__2" },
        { "value": "landscape", "label": "t:sections.all.image.aspect_ratio.options__3" }
      ]
    },
    {
      "type": "select",
      "id": "interaction_behavior",
      "label": "t:sections.expandable_card.settings.interaction_behavior.label",
      "default": "hover",
      "options": [
        {
          "value": "hover",
          "label": "t:sections.expandable_card.settings.interaction_behavior.options.hover"
        },
        {
          "value": "drawer",
          "label": "t:sections.expandable_card.settings.interaction_behavior.options.drawer"
        }
      ]
    },
    {
      "type": "header",
      "content": "t:sections.all.carousel.header"
    },
    {
      "type": "range",
      "id": "items_per_row",
      "label": "t:sections.all.columns_desktop.label",
      "min": 1,
      "max": 4,
      "step": 0.5,
      "default": 3
    },
    {
      "type": "select",
      "id": "column_gap",
      "label": "t:sections.all.padding.column_gap.label",
      "default": "medium",
      "options": [
        { "value": "no_gap", "label": "t:sections.all.padding.column_gap.no_gap" },
        { "value": "small", "label": "t:sections.all.padding.column_gap.small" },
        { "value": "medium", "label": "t:sections.all.padding.column_gap.medium" },
        { "value": "large", "label": "t:sections.all.padding.column_gap.large" }
      ]
    },
    {
      "type": "checkbox",
      "id": "show_navigation",
      "label": "t:sections.all.carousel.show_navigation.label",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_pagination",
      "label": "t:sections.all.carousel.show_pagination.label",
      "default": false,
      "info": "t:sections.all.carousel.show_pagination.info"
    },
    {
      "type": "checkbox",
      "id": "slide_auto_change",
      "label": "t:sections.all.carousel.slide_auto_change",
      "default": false
    },
    {
      "type": "range",
      "id": "change_slide_every",
      "label": "t:sections.all.carousel.change_slide_every",
      "min": 2,
      "max": 10,
      "step": 1,
      "unit": "s",
      "default": 5
    },
    {
      "type": "checkbox",
      "id": "reveal_slide",
      "label": "t:sections.all.carousel.reveal_slide.label",
      "default": true
    },
    {
      "type": "header",
      "content": "t:sections.all.mobile_options.label"
    },
    {
      "type": "range",
      "id": "items_per_row_mobile",
      "label": "t:sections.all.items.items_per_row_mobile",
      "min": 1,
      "max": 2,
      "step": 0.1,
      "default": 1.2
    },
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "t:sections.all.padding.padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "default": 80
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "t:sections.all.padding.padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "default": 80
    },
    {
      "type": "header",
      "content": "t:sections.all.divider.section_divider_heading"
    },
    {
      "type": "checkbox",
      "id": "show_divider",
      "label": "t:sections.all.divider.show_divider",
      "default": false
    }
  ],
  "blocks": [
    {
      "type": "layered_card",
      "name": "t:sections.expandable_card.blocks.layered_card.name",
      "settings": [
        {
          "type": "header",
          "content": "t:sections.expandable_card.blocks.layered_card.settings.media_header"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "t:sections.all.image.label"
        },
        {
          "type": "image_picker",
          "id": "image_mobile",
          "label": "t:sections.all.image.mobile_image"
        },
        {
          "type": "header",
          "content": "t:sections.all.content_setting.label"
        },
        {
          "type": "text",
          "id": "subheading",
          "label": "t:sections.all.content_setting.subheading.label"
        },
        {
          "type": "text",
          "id": "heading",
          "label": "t:sections.all.content_setting.heading.label",
          "default": "Card heading"
        },
        {
          "type": "richtext",
          "id": "description",
          "label": "t:sections.expandable_card.blocks.layered_card.settings.description",
          "default": "<p>Add a short description for this card.</p>"
        },
        {
          "type": "page",
          "id": "page",
          "label": "t:sections.expandable_card.blocks.layered_card.settings.drawer_page"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "t:sections.expandable_card.name",
      "category": "t:sections.group.storytelling",
      "blocks": [
        {
          "type": "layered_card",
          "settings": {
            "heading": "Heading",
            "subheading": "Subheading",
            "description": "<p>Add a short description for this card.</p>"
          }
        },
        {
          "type": "layered_card",
          "settings": {
            "heading": "Heading",
            "subheading": "Subheading",
            "description": "<p>Add a short description for this card.</p>"
          }
        },
        {
          "type": "layered_card",
          "settings": {
            "heading": "Heading",
            "subheading": "Subheading",
            "description": "<p>Add a short description for this card.</p>"
          }
        }
      ]
    }
  ]
}
{% endschema %}
